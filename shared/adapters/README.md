# Adapters

Shared adapters belong here when they are reusable across multiple chapters.

Two adapter families are expected:
- `vendor_compatible/` for original-schema workflows
- `canonical/` for cross-project shared workflows

Chapter-local transformation code should remain outside this folder until it proves reusable.
