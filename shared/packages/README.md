# Packages

Reusable public-facing library code should live here.

Initial target:
- one lightweight Python package for loaders, validators, and chapter adapters

Boundary rule:
- this folder should support the notebook-style examples in `examples/`
- it should not become a second home for the current internal engineering code
