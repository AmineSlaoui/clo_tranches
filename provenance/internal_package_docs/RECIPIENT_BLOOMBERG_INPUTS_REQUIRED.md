# Recipient Bloomberg Inputs Required

This package intentionally does not include Bloomberg CLO contract rows.

The code, templates, and staging script are included. To run the live CLO
contract path, the recipient must export the required CLO tranche, collateral,
and manager files from Bloomberg and stage them with the included air-gap
playbook. The packaged templates define the required columns and file names.
