# Outputs

Run `run_sample.ps1` to create `outputs/sample`.

Run `run_live_template.ps1` with the required live-data argument to create `outputs/live`.

Project: 4.1 Correlation & Tail Risk in CLO Tranches
